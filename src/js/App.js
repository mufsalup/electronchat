import React from "react";

function App() {
    const title = "Hello World!";
    const enhandedTitle = title + ' - React App';

    return (
        <h1>{enhandedTitle}</h1>
    );
}

export default App;